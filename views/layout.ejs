<% include ./_header %>

  <% if (user) { %>
    <% include ./_nav %>
  <% } %>

    <!-- should not be a nav bar on home page in final version...it's redundant-->
    <div class="container">
        <%- body %>
    </div>

<% include ./_footer %>
